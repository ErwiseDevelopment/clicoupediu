<IfModule mod_rewrite.c>
    Options -MultiViews
    RewriteEngine On
    
    # Força o redirecionamento para o index.php se não for arquivo ou pasta real
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]
</IfModule>